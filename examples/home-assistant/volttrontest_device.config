{
  "driver_config": {
    "ip_address": "${HOME_ASSISTANT_IP}",
    "access_token": "${HOME_ASSISTANT_TOKEN}",
    "port": "${HOME_ASSISTANT_PORT}"
  },
  "driver_type": "home_assistant",
  "registry_config": "config://volttrontest_registry.json",
  "interval": 30,
  "timezone": "UTC"
}
